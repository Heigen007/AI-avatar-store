const config = {
    appId: 'org.repliky.quasar.app',
    appName: 'Repliky',
    webDir: 'dist/capacitor',
    bundledWebRuntime: false,

    // 🔹 Цвет фона WebView (safe area полосы)
    backgroundColor: '#091a2c'
}

export default config
